<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Home</title>
</head>
<body>
  <h1>Selamat Datang</h1>
  <% blogPosts.forEach(function (article) { %>
    <div>
      <h2><%= article.title %></h2>
      <p><%= article.body %></p>
    </div>
  <% }) %>
</body>
</html>